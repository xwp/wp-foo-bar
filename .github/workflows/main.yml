name: Lint and Test

on: [push]

env:
  DEV_LIB_PATH: vendor/xwp/wp-dev-lib/scripts

jobs:
  # lint:
  #   name: 'Lint'
  #   runs-on: ubuntu-latest

  #   steps:
  #   - uses: actions/checkout@v2
  #   - name: Use Node.js
  #     uses: actions/setup-node@v1
  #     with:
  #       node-version: '14'
  #   - run: npm ci
  #   - run: npm run lint

  # test-e2e-coverage:
  #   needs: [lint]
  #   name: 'E2E tests (PHP 7.4, WordPress latest, with code coverage)'
  #   runs-on: ubuntu-latest

  #   env:
  #     NODE_ENV: e2e
  #     WP_VERSION: 5.7

  #   steps:
  #   - uses: actions/checkout@v2
  #   - uses: hmarr/debug-action@v2

  #   - name: Use Node.js
  #     uses: actions/setup-node@v1
  #     with:
  #       node-version: '14'
  #   - run: npm ci
  #   - run: npm run env:start
  #   - run: npm run wp -- wp core install --title=WordPress --admin_user=admin --admin_password=password --admin_email=admin@example.com --skip-email --url=http://localhost:8088 --quiet
  #   - run: npm run wp -- wp plugin activate foo-bar
  #   - run: npm run build:js
  #   - run: npm run test:e2e:coverage

  #   - name: Coveralls
  #     uses: coverallsapp/github-action@master
  #     with:
  #       github-token: ${{ secrets.GITHUB_TOKEN }}
  #       path-to-lcov: ./tests/coverage/e2e/lcov.info
  #       parallel: true

  # test-js-coverage:
  #   needs: [lint]
  #   name: 'JS unit tests (with code coverage)'
  #   runs-on: ubuntu-latest

  #   steps:
  #   - uses: actions/checkout@v2
  #   - uses: hmarr/debug-action@v2

  #   - name: Use Node.js
  #     uses: actions/setup-node@v1
  #     with:
  #       node-version: '14'
  #   - run: npm ci
  #   - run: npm run test:js:coverage

  #   - name: Coveralls
  #     uses: coverallsapp/github-action@master
  #     with:
  #       github-token: ${{ secrets.GITHUB_TOKEN }}
  #       path-to-lcov: ./tests/coverage/js/lcov.info
  #       parallel: true

  test-php-coverage:
    # needs: [lint]
    name: 'PHP tests (PHP 7.4, WordPress latest, with code coverage)'
    runs-on: ubuntu-latest

    env:
      WP_VERSION: latest

    # services:
    #   mysql:
    #     image: mysql:5
    #     env:
    #       MYSQL_ROOT_PASSWORD: root
    #       MYSQL_DATABASE: wordpress_test
    #     ports:
    #       - 3306
    #     options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    - uses: actions/checkout@v2
    - name: Setup PHP 7.4
      uses: shivammathur/setup-php@v2
      with:
        php-version: '7.4'
        tools: phpunit
    - run: sudo systemctl enable mysql.service
    - run: sudo systemctl start mysql.service
    - run: composer install
    - run: source bin/install-wp-tests.sh wordpress_test root root localhost latest
    - run: cd "$INSTALL_PATH" && composer test

  # finish:
  #   needs: [test-e2e-coverage, test-js-coverage]
  #   name: Finish
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Coveralls Finished
  #     uses: coverallsapp/github-action@master
  #     with:
  #       github-token: ${{ secrets.github_token }}
  #       parallel-finished: true
